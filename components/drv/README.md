# drv

Grouping directory for peripheral IC drivers, each as its own component (e.g. `drv_st7789`, `drv_kbd`, `drv_usbmidi`). Not a component itself — see `bsp/` and `sys/` for the pattern: `components/<layer>/<layer>_<name>/`.
